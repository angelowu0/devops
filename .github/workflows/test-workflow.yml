name: test-workflow
on:
  push:
    branches:
      - 'feature/*'
  pull_request:
    branches:
      - 'main'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run build Process
        run: make build